"use strict";
const assert=require("node:assert/strict"),{Budget}=require("../../kernel-src/http-server.js");
const b=new Budget(),windows=[];for(let listener=1;listener<=200;listener++){const start=process.hrtime.bigint();for(let i=0;i<50;i++){const id=b.reserve(listener,"connections",1,100);assert.ok(id);assert.equal(b.release(id),true);}windows.push(Number(process.hrtime.bigint()-start)/1e6);}const ms=windows.reduce((a,x)=>a+x,0),early=windows.slice(0,20).reduce((a,x)=>a+x,0),late=windows.slice(-20).reduce((a,x)=>a+x,0);assert.equal(b.snapshot().reservations,0);assert.ok(ms<1000,`indexed budget scale ${ms}ms`);assert.ok(late<Math.max(early*10,20),`late listener window regressed: early=${early} late=${late}`);console.log(JSON.stringify({listeners:200,operations:10000,ms,early,late}));
